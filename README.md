# Scientific Calculator

Next.js 14 (App Router) + TypeScript + Tailwind CSS scientific calculator using **mathjs** for expression evaluation.

## Setup

```bash
npm install
npm run dev
```

## PWA icons

The manifest references `/icon-192.png` and `/icon-512.png`. Add those files to `public/` if you want proper "Add to Home Screen" icons.

